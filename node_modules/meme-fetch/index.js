const fetch = require("node-fetch");

exports.getImage = async function() {
    const res = await fetch(`https://meme-api.herokuapp.com/gimme/memes`)
    const { url } = res.json()
    return `${url}`;
}